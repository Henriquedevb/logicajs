/*
//escreva um algoritmo para imprimir na tela os números de 1 a 5
algoritmo "enquanto"

var
   num : inteiro
inicio
      num <- 0
      repita
            num <- num + 1
            escreval(num)
      ate (num > 4)
fimalgoritmo
*/

let num;
num = 0;
do{
    num++
    console.log(num)
}while(num <= 4);
